summary
I'm using node.js to solve the coda maze via bfs. When doing http request, I implemented retries (max retries = 10) for failed non-specified status code (e.g. 503, 500)

how-to
1. OPEN a command prompt on this directory
2. TYPE node index.js

ps
For more verbose log, TYPE node index.js -d